#!/bin/bash
#
# Run the project locally, in whatever form 'locally' takes here.
#
# Replace the body, keep the name: CI and every contributor call this file, and
# neither should have to know what is underneath it.

set -euo pipefail

echo "script/server is not wired up yet" >&2
exit 1
