# OS
.DS_Store
Thumbs.db

# Editors and tools
.vscode
.history
.idea
.claude

# Yours: the dependency directory and the build output. Ignore what a build
# produces, commit what a release ships only if consumers install from the repo.
# node_modules/
# dist/
